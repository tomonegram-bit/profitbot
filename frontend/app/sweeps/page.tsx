'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function SweepsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Sweeps</Typography>
      <Typography sx={{ mt: 2 }}>This is a placeholder for the Sweeps page.</Typography>
    </Box>
  );
}
