'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function LotsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Lock Lots</Typography>
      <Typography sx={{ mt: 2 }}>This is a placeholder for the Lock Lots page.</Typography>
    </Box>
  );
}
