'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ClientDepositsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">My Deposits</Typography>
      <Typography sx={{ mt: 2 }}>Client deposits placeholder.</Typography>
    </Box>
  );
}
