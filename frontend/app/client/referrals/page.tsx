'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ClientReferralsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">Referrals</Typography>
      <Typography sx={{ mt: 2 }}>Client referrals placeholder.</Typography>
    </Box>
  );
}
