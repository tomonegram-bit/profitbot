'use client';

import React from 'react';
import { Box, Typography } from '@mui/material';

export default function ClientPayoutsPage() {
  return (
    <Box sx={{ p: 4 }}>
      <Typography variant="h4">My Payouts</Typography>
      <Typography sx={{ mt: 2 }}>Client payouts placeholder.</Typography>
    </Box>
  );
}
